services:
  app:
    build: .
    ports:
      - "3000:3000"
    depends_on:
      - mongo
    environment:
      - MONGO_URI=mongodb://mongo:27017/practica-docker
    networks:
      - api-network
  
networks:
  api-network:
    driver: bridge

volumes:
  mongo-data: